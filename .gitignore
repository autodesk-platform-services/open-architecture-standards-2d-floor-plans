# MkDocs build artifacts
site/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Claude Code — local per-user permissions and state (never commit)
# Both files are local-only; the harness writes session permissions to settings.json,
# so it must stay untracked to avoid leaking absolute paths / usernames.
.claude/settings.json
.claude/settings.local.json
